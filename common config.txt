Thin Client
	config.Database_Instance, config.Database_Name, config.Database_User_Name, config.Database_Password

Stand Alone
	Nothing required system does it itself as integrated security but LOCALDB mustbe installed

Multi Client Server
	if (Attach Database File)
		config.Database_Instance, config.Database_Name, config.Database_User_Name, config.Database_Password +
		config.Local_Database_File_Name

	if (No attachment required) (like thin client)
		config.Database_Instance, config.Database_Name, config.Database_User_Name, config.Database_Password


Database Wizard Steps
- Choose Terminal Function Type
	Thin Client
	Stand Alone
	Multi Client Server
- Thin Client
	Server IP : ?
	Database Name : [DTRM Simple] (Not Required)
	Database Username : [sa] (if sa Not Required) ?
	Database Password : ?
- Stand Alone
	Check LocalDB installed ?
- Multi Client Server
	Server IP : [.] (Not Required)
	Database Name : [DTRM Simple] (Not Required)
	Database Username : [sa] (if sa Not Required) ?
	Database Password : ?






